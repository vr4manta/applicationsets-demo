---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  annotations:
    apps.open-cluster-management.io/reconcile-rate: high
  name: ztp-repository
  namespace: ztp-clusters
spec:
  type: Git
  pathname: 'https://github.com/vr4manta/applicationsets-demo.git'
